<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/white"
    >

    <LinearLayout
        android:id="@+id/item_top"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/dp10">

        <TextView
            android:id="@+id/ir_url_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="网址"
            android:textColor="@color/regex_title_color"
            android:textSize="16sp"/>

        <EditText
            android:id="@+id/ir_regex_et"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/dp10"
            android:background="@drawable/shape_round_bg"
            android:maxLines="3"
            android:minHeight="@dimen/dp48"
            android:paddingLeft="@dimen/dp10"
            android:text="123456"
            android:textColor="@color/regex_content_color"/>
    </LinearLayout>

    <LinearLayout
        android:id="@+id/item_action"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/item_top"
        android:orientation="horizontal">

        <Button
            android:id="@+id/ir_change_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@null"
            android:drawableLeft="@drawable/ic_edit_slt"
            android:gravity="center_vertical"
            android:text="修改"
            android:textColor="@color/regex_title_color"/>

        <Button
            android:id="@+id/ir_del_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/dp10"
            android:background="@null"
            android:drawableLeft="@drawable/ic_delete_slt"
            android:gravity="center_vertical"
            android:text="删除"
            android:textColor="@color/regex_title_color"/>
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp10"
        android:layout_below="@+id/item_action"
        android:background="@color/regex_bg_color"></LinearLayout>
</RelativeLayout>